# BSD-007 外部インターフェース基本設計書 テンプレート

## セクション構成

```markdown
## 1. 外部インターフェース設計方針

### 1.1 対象外部システム
（REQ-007 の外部システム・インターフェース一覧から）

### 1.2 共通方針
- タイムアウト設定: （例: 接続タイムアウト 5秒、読み取りタイムアウト 30秒）
- リトライ方針: （例: 最大3回、指数バックオフ）
- サーキットブレーカー: 採用 / 非採用
- エラー通知: （例: Slack / メール通知の条件）

---

## 2. 外部システム一覧

| 外部システムID | システム名 | 役割 | 通信方向 | プロトコル |
|---|---|---|---|---|
| EXT-001 | | | 送信 / 受信 / 双方向 | REST / SOAP / gRPC |

---

## 3. 外部インターフェース詳細

### 3.1 {外部システムID}: {システム名}

**概要**: {REQ-007 から}
**提供元/提供先**: {組織名}
**通信方向**: 送信 / 受信 / 双方向

**接続情報:**
| 項目 | 内容 |
|---|---|
| エンドポイント | `https://api.example.com/v1` |
| プロトコル | HTTPS / REST |
| 認証方式 | APIキー / OAuth 2.0 / Basic認証 |
| データフォーマット | JSON / XML / CSV |
| 文字コード | UTF-8 |

**インターフェース一覧:**
| No. | メソッド | エンドポイント/機能名 | 概要 | タイムアウト |
|---|---|---|---|---|
| 1 | GET | `/resource` | データ取得 | 30秒 |

**データフォーマット概要（リクエスト）:**
```json
{
  "key": "value"
}
```

**データフォーマット概要（レスポンス）:**
```json
{
  "result": "success",
  "data": { ... }
}
```

**エラー処理方針:**
| エラー種別 | 対処方法 |
|---|---|
| タイムアウト | リトライ（最大3回） |
| 接続拒否 | アラート通知・処理中断 |
| ビジネスエラー | エラーログ記録・ユーザーへのメッセージ表示 |

**連携頻度・データ量:**
- 呼び出し頻度: {常時 / バッチ（スケジュール） / ユーザー操作時}
- 想定データ量/回: {件数・データサイズ}

（外部システム数分繰り返す）

---

## 4. 後続フェーズへの影響

| 影響先 | 内容 |
|---|---|
| DSD-005 | 外部IF詳細仕様（全パラメータ・バリデーション・エラーコード） |
| IT-001 | 外部システム連携の結合テスト |
```
